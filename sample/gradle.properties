org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
kotlin.code.style=official

# OctetSDK version pin. The release-android.yml workflow rewrites this
# line on every tagged release to match the just-published SDK.
octetSdkVersion=0.0.3-alpha
